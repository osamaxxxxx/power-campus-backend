using Microsoft.AspNetCore.SignalR;

namespace webBackendGP.Hubs
{
    public class AttendanceHub : Hub
    {
        // Clients will connect and listen to events here.
        // The server will broadcast events directly using IHubContext.
    }
}
